[project]
name = "reviewarmour"
version = "0.1.0"
description = "ReviewArmour AI layer: commercial reasoning, self-correction, conversation"
requires-python = ">=3.10"
dependencies = [
  "anthropic>=0.40.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=7.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
